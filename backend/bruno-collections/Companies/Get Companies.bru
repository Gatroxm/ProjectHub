meta {
  name: Get Companies
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/companies
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("should return 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}